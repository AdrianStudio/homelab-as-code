apiVersion: v1
kind: Service
metadata:
  name: nginx-svc
  namespace: tests
  labels:
     app: nginx
spec:
  type: NodePort
  ports:
    - port:  80
      targetPort: 80
      nodePort: 30081
  selector:
    app: nginx-app
